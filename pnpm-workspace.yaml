packages:
  - "clients/*"
allowBuilds:
  # Optional native speedups are not required for SDK correctness. Keep their
  # install scripts disabled to minimize clean-checkout supply-chain execution.
  bufferutil: false
  utf-8-validate: false
